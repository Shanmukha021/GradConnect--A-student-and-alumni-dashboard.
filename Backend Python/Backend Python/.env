# FastAPI + PostgreSQL + Redis
# Local development defaults (override as needed)

# For local runs
DATABASE_URL=postgres://avnadmin:AVNS_ijI3ds00Un08fJ93tvv@gradconnect-shanmukhakalangi-bbb3.g.aivencloud.com:16685/defaultdb

# For Docker Compose (db service)
# DATABASE_URL=postgresql://postgres:password@db:5432/alumni_platform

# JWT settings
JWT_SECRET=change-me-super-secret-key
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=7d

# Uploads (served at /uploads)
UPLOAD_DIR=./backend/uploads

# Optional: Redis URL for caching / sessions
REDIS_URL=redis://localhost:6379/0
# For Docker Compose
# REDIS_URL=redis://redis:6379/0

# Environment and logging
ENV=development
LOG_LEVEL=info

# CORS (comma-separated). Use * for permissive dev.
CORS_ALLOW_ORIGINS=*